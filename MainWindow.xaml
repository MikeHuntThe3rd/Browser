<Window x:Class="Browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Browser"
        xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
    </Window.Resources>
    <Grid x:Name="grid" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TabControl x:Name="tabs">
            <TabControl.Template>
                <ControlTemplate TargetType="TabControl">
                    <Grid Background="#EEE" MouseDown="Drag">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TabPanel x:Name="HeaderPanel" IsItemsHost="True" Grid.Column="0"/>

                            <StackPanel Orientation="Horizontal" 
                           Grid.Column="1">
                                <Button Content="+" Click="NewTab_Click"/>
                                <Button Content="—" Click="Minimize_Click"/>
                                <Button Content="□" Click="Maximize_Click"/>
                                <Button Content="X" Click="Close_Click"/>
                            </StackPanel>
                        </Grid>

                        <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" Grid.Row="1"/>
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>

            <TabControl.Resources>
                <DataTemplate x:Key="tab_header_button_wrapper">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="51"/>
                            <ColumnDefinition Width="30"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="window" HorizontalAlignment="Left" />
                        <Button x:Name="colse_tab" Grid.Column="1" Content="cls" HorizontalAlignment="Stretch" Click="colse_tab_Click"/>
                    </Grid>
                </DataTemplate>
            </TabControl.Resources>
            <TabItem HeaderTemplate="{StaticResource tab_header_button_wrapper}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button x:Name="back" Grid.Row="0" Grid.Column="0" Content="Back" IsEnabled="False" Click="back_Click"/>
                    <Button x:Name="foward" Grid.Row="0" Grid.Column="1" Content="Forward" IsEnabled="False" Click="foward_Click"/>
                    <Button x:Name="refresh" Grid.Row="0" Grid.Column="2" Content="Refresh" IsEnabled="False" Click="refresh_Click"/>
                    <TextBox x:Name="URL" Grid.Row="0" Grid.Column="3" KeyDown="UrlInput"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
