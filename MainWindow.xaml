<Window x:Class="Browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Browser"
        xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        xmlns:dragablz="clr-namespace:Dragablz;assembly=Dragablz"
        mc:Ignorable="d"
        WindowStyle="None"
        Loaded="Window_Loaded"
        Background="#EEE"
        ResizeMode="CanResize"
        Title="MainWindow"
        MinWidth="450">

    <Window.Resources>
        <!--<ControlTemplate TargetType="{x:Type dragablz:TabablzControl}" x:Key="asd">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="36" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <dragablz:DragablzItemsControl x:Name="PART_HeaderItemsControl" Grid.Row="0" />
                <Border Grid.Row="1">
                    <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" Grid.Row="1" Margin="8"/>
                </Border>
            </Grid>
        </ControlTemplate>-->
        <local:HeaderResize x:Key="resizer" />
        <Style TargetType="Button" x:Key="WindowControlButtonStyle">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="2,3,2,3"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E8E8E8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C0C0C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type Thumb}" x:Key="InvisibleThumbStyle">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid Background="{TemplateBinding Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ContentPresenter" x:Key="selection">
        </Style>

    </Window.Resources>
    <Grid x:Name="Tabgrid">
        <dragablz:TabablzControl x:Name="tabs" IsTabStop="False" ShowDefaultAddButton="False" ShowDefaultCloseButton="False">
            
            <!--<dragablz:TabablzControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1"/>
                </ItemsPanelTemplate>
            </dragablz:TabablzControl.ItemsPanel>-->
            
            <dragablz:TabablzControl.ItemContainerStyle>
                <Style TargetType="{x:Type dragablz:DragablzItem}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate>
                                <Border BorderThickness="{TemplateBinding Border.BorderThickness}" Padding="{TemplateBinding Control.Padding}" BorderBrush="{TemplateBinding Border.BorderBrush}" Background="{TemplateBinding Panel.Background}" SnapsToDevicePixels="True">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                        </Grid.RowDefinitions>
                                        <Thumb Grid.Row="1" Width="0" Height="0" x:Name="PART_Thumb" />
                                        <ContentPresenter Grid.Row="2" Content="{TemplateBinding ContentControl.Content}" ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}" ContentStringFormat="{TemplateBinding ContentControl.ContentStringFormat}" HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                                    </Grid>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </dragablz:TabablzControl.ItemContainerStyle>
            
            <dragablz:TabablzControl.HeaderItemTemplate>
                <DataTemplate>
                    <Grid Background="Transparent">
                        <Border Background="White"
                                Padding="10"
                                CornerRadius="10,10,0,0"
                                BorderThickness="0"
                                Height="36">
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="30"/>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{Binding Icon, Mode=OneWay}" />
                                        <TextBlock Margin="5, 0" Text="{Binding Title ,Mode=OneWay}" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                    <Thumb dragablz:DragablzItem.IsCustomThumb="True" Style="{DynamicResource InvisibleThumbStyle}"/>
                                </Grid>
                                <Button Grid.Column="1" Content="×" Command="dragablz:TabablzControl.CloseItemCommand" Background="Transparent" BorderThickness="0" Foreground="#B22222" FontWeight="Bold" Cursor="Hand"/>
                            </Grid>
                        </Border>
                    </Grid>
                </DataTemplate>
            </dragablz:TabablzControl.HeaderItemTemplate>

            <dragablz:TabablzControl.ContentTemplate>
                <DataTemplate DataType="{x:Type local:Tab}">
                    <local:TabContents />
                </DataTemplate>
            </dragablz:TabablzControl.ContentTemplate>
            
            <dragablz:TabablzControl.HeaderSuffixContent>
                <Grid Background="#EEE" MouseDown="Drag" x:Name="DragBar">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="60" />
                            <ColumnDefinition Width="60" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>
                        <Button Style="{StaticResource WindowControlButtonStyle}" Grid.Column="0" HorizontalAlignment="Left" Content="+" ToolTip="Új lap" Click="NewTab_Click" Margin="0,0,4,0"/>
                        <Button Style="{StaticResource WindowControlButtonStyle}" Grid.Column="1" Content="—" ToolTip="Minimalizál" Click="Minimize_Click" Margin="0,0,4,0"/>
                        <Button Style="{StaticResource WindowControlButtonStyle}" Grid.Column="2" Content="□" ToolTip="Maximalizál" Click="Maximize_Click" Margin="0,0,4,0"/>
                        <Button Style="{StaticResource WindowControlButtonStyle}" Grid.Column="3" Content="X" ToolTip="Bezár" Click="Close_Click" Foreground="#B22222"/>
                </Grid>
            </dragablz:TabablzControl.HeaderSuffixContent>
        </dragablz:TabablzControl>
    </Grid>
</Window>
