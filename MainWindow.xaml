<Window x:Class="Browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Browser"
        xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        mc:Ignorable="d"
        WindowStyle="None"
        Background="Transparent"
        ResizeMode="CanResize"
        Loaded="Window_Loaded"
        Title="MainWindow">
    <Window.Resources>
    </Window.Resources>
    <Grid x:Name="Tabgrid">
        <TabControl x:Name="tabs">
            <TabControl.Template>
                <ControlTemplate TargetType="TabControl">
                    <Grid Background="#EEE" MouseDown="Drag">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="5*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TabPanel x:Name="HeaderPanel" IsItemsHost="True" Grid.Column="0"/>
                            <Grid Grid.Column="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Content="+" Grid.Column="0" HorizontalAlignment="Stretch" Click="NewTab_Click"/>
                                <Button Content="—" Grid.Column="1" HorizontalAlignment="Stretch" Click="Minimize_Click"/>
                                <Button Content="□" Grid.Column="2" HorizontalAlignment="Stretch" Click="Maximize_Click"/>
                                <Button Content="X" Grid.Column="3" HorizontalAlignment="Stretch" Click="Close_Click"/>
                            </Grid>
                        </Grid>
                        <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" Grid.Row="1"/>
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>

            <TabControl.Resources>
                <DataTemplate x:Key="tab_header_button_wrapper">
                    <Grid MouseDown="Tab_Drag">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="window" HorizontalAlignment="Left" />
                        <Button x:Name="colse_tab" Grid.Column="1" Content="cls" HorizontalAlignment="Stretch" Click="close_tab_Click"/>
                    </Grid>
                </DataTemplate>
            </TabControl.Resources>
        </TabControl>
    </Grid>
</Window>
